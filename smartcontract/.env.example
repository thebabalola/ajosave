# Environment Variables for AjoSave Smart Contract Deployment
# Copy this file to .env and fill in your actual values
# NEVER commit your .env file - it's already in .gitignore

# ============================================
# REQUIRED - Private Key for Deployment
# ============================================
# Your wallet private key (0x prefix + 64 hex characters = 66 total characters)
# Example: PRIVATE_KEY=0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
# ⚠️ IMPORTANT: 
# - No quotes around the value
# - No spaces around the = sign
# - Must start with 0x
# - Must be exactly 66 characters
PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE

# ============================================
# OPTIONAL - Network RPC URLs
# ============================================
# If you want to override the default RPC URLs defined in foundry.toml
# Default networks are already configured:
# - celo-sepolia: https://forno.celo-sepolia.celo-testnet.org/
# - celo: https://forno.celo.org
# - base-sepolia: https://sepolia.base.org
# - base: https://mainnet.base.org

# Custom RPC URL (optional - overrides foundry.toml settings)
# RPC_URL=https://your-custom-rpc-url.com

# ============================================
# OPTIONAL - Contract Addresses (for existing deployments)
# ============================================
# These are optional and only needed if you're deploying with existing token/treasury
# The deployment scripts will use hardcoded values if these are not set

# ERC20 Token Address (if deploying with existing token)
# TOKEN_ADDRESS=0x0000000000000000000000000000000000000000

# Treasury Address (where fees are sent)
# Default: 0xa91D5A0a64ED5eeF11c4359C4631279695A338ef
# TREASURY_ADDRESS=0xa91D5A0a64ED5eeF11c4359C4631279695A338ef

# ============================================
# NOTES
# ============================================
# 1. For local testing with Anvil:
#    - Start Anvil: anvil
#    - Use RPC_URL=http://localhost:8545
#    - Use one of Anvil's default private keys for testing
#
# 2. Network names you can use:
#    - celo-sepolia (Celo Sepolia Testnet)
#    - celo (Celo Mainnet)
#    - base-sepolia (Base Sepolia Testnet)
#    - base (Base Mainnet)
#
# 3. Deployment scripts will automatically load these variables
#    - deploy.sh (Linux/Mac)
#    - deploy.ps1 (Windows PowerShell)
#    - deploy-base.sh (Base network specific)
